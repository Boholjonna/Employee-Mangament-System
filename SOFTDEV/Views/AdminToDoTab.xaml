<UserControl x:Class="SOFTDEV.Views.AdminToDoTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SOFTDEV.ViewModels"
             Background="{StaticResource DarkBackgroundBrush}">

    <UserControl.Resources>

        <!-- PillTextBoxStyle -->
        <Style x:Key="PillTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground"  Value="White" />
            <Setter Property="CaretBrush"  Value="White" />
            <Setter Property="FontSize"    Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="#4a447d"
                                CornerRadius="25"
                                Padding="20,10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PillComboBoxStyle -->
        <Style x:Key="PillComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize"   Value="14" />
            <!-- Style each dropdown item: white text, hover highlight -->
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground"  Value="White" />
                        <Setter Property="Background"  Value="Transparent" />
                        <Setter Property="Padding"     Value="16,8" />
                        <Setter Property="Cursor"      Value="Hand" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemBd"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBd" Property="Background" Value="#6a5acd" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBd" Property="Background" Value="#7b6ec8" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- ToggleButton covers the entire pill and toggles the dropdown on click -->
                            <ToggleButton x:Name="ToggleBtn"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                      RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False"
                                          Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Pill"
                                                Background="#4a447d"
                                                CornerRadius="25"
                                                Padding="20,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <!-- selected value or placeholder -->
                                                <Grid Grid.Column="0">
                                                    <TextBlock x:Name="SelectedText"
                                                               Text="{Binding SelectionBoxItem,
                                                                      RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                               Foreground="White"
                                                               VerticalAlignment="Center"
                                                               Visibility="Visible" />
                                                    <TextBlock x:Name="Placeholder"
                                                               Text="{Binding Tag,
                                                                      RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                               Foreground="#aaaaaa"
                                                               VerticalAlignment="Center"
                                                               IsHitTestVisible="False"
                                                               Visibility="Collapsed" />
                                                </Grid>
                                                <!-- arrow -->
                                                <TextBlock Grid.Column="1"
                                                           Text="▾"
                                                           Foreground="White"
                                                           VerticalAlignment="Center"
                                                           Margin="8,0,0,0" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Pill" Property="Background" Value="#5a5490" />
                                            </Trigger>
                                            <!-- hide placeholder when an item is selected -->
                                            <DataTrigger Binding="{Binding SelectedItem,
                                                         RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                         Value="{x:Null}">
                                                <Setter TargetName="Placeholder"  Property="Visibility" Value="Visible" />
                                                <Setter TargetName="SelectedText" Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Dropdown popup -->
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   StaysOpen="False">
                                <Border Background="#4a447d"
                                        CornerRadius="12"
                                        Padding="4"
                                        MinWidth="{Binding ActualWidth,
                                                   RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ActionButtonStyle -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background"  Value="#8b7ed6" />
            <Setter Property="Foreground"  Value="Black" />
            <Setter Property="FontWeight"  Value="Bold" />
            <Setter Property="FontSize"    Value="14" />
            <Setter Property="Cursor"      Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="25"
                                Padding="32,12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#7b6ec8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#3a3560" />
                                <Setter TargetName="Bd" Property="Opacity"    Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- UtilityButtonStyle -->
        <Style x:Key="UtilityButtonStyle" TargetType="Button">
            <Setter Property="Background"  Value="#554a9e" />
            <Setter Property="Foreground"  Value="White" />
            <Setter Property="FontSize"    Value="12" />
            <Setter Property="Opacity"     Value="0.7" />
            <Setter Property="Cursor"      Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                Padding="14,6"
                                Opacity="{TemplateBinding Opacity}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="1.0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Row 0: header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!-- Col 0: Greeting -->
            <TextBlock Grid.Column="0"
                       Text="{Binding AdminName, StringFormat='Hello {0}! 👋'}"
                       Foreground="#a294f9"
                       FontSize="13"
                       VerticalAlignment="Center"
                       Margin="60,12,0,8"
                       AutomationProperties.Name="Greeting" />
            <!-- Col 1: Utility buttons -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,12,60,8">
                <Button Content="FILTER ▾"
                        Style="{StaticResource UtilityButtonStyle}"
                        Command="{Binding FilterCommand}"
                        AutomationProperties.Name="Filter Tasks"
                        Margin="0,0,8,0" />
                <Button Content="SORT ▾"
                        Style="{StaticResource UtilityButtonStyle}"
                        Command="{Binding SortCommand}"
                        AutomationProperties.Name="Sort Tasks" />
            </StackPanel>
        </Grid>

        <!-- Row 1: section headers -->
        <StackPanel Grid.Row="1" Margin="60,0,60,8">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <Button x:Name="BackButton"
                        Content="← Back"
                        Style="{StaticResource UtilityButtonStyle}"
                        Opacity="1"
                        Margin="0,0,12,0"
                        Click="BackButton_Click"
                        AutomationProperties.Name="Back to Dashboard" />
                <TextBlock Text="Task Management"
                           Foreground="White"
                           FontWeight="Bold"
                           FontSize="20"
                           VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock Text="ADD TASK:"
                       Foreground="White"
                       FontSize="16"
                       FontFamily="Georgia"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- Row 2: task form -->
        <StackPanel x:Name="Task_Form"
                    Grid.Row="2"
                    Margin="60,0,60,12">

            <!-- Title field -->
            <TextBlock x:Name="TitleLabel"
                       Text="Task Title: (required)"
                       Foreground="White"
                       FontSize="13"
                       Margin="0,0,0,4" />
            <TextBox Style="{StaticResource PillTextBoxStyle}"
                     Text="{Binding TaskTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12"
                     AutomationProperties.LabeledBy="{Binding ElementName=TitleLabel}" />

            <!-- Description field -->
            <TextBlock x:Name="DescLabel"
                       Text="Description: (mandatory)"
                       Foreground="White"
                       FontSize="13"
                       Margin="0,0,0,4" />
            <TextBox Style="{StaticResource PillTextBoxStyle}"
                     Text="{Binding TaskDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     MinHeight="60"
                     Margin="0,0,0,12"
                     AutomationProperties.LabeledBy="{Binding ElementName=DescLabel}" />

            <!-- Assignee + DueDate row -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Assigned To -->
                <ComboBox Grid.Column="0"
                          Style="{StaticResource PillComboBoxStyle}"
                          Tag="Assigned to:"
                          ItemsSource="{Binding EmployeeNames}"
                          SelectedItem="{Binding AssignedTo, Mode=TwoWay}"
                          Margin="0,0,8,0"
                          x:Name="AssigneeCombo"
                          AutomationProperties.Name="Assigned To" />

                <!-- Due Date -->
                <Border Grid.Column="1"
                        Background="#4a447d"
                        CornerRadius="25"
                        Padding="20,0">
                    <Grid>
                        <!-- Watermark label — hidden once a date is picked -->
                        <TextBlock Text="Due Date"
                                   Foreground="#aaaaaa"
                                   FontSize="14"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   Panel.ZIndex="1">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedDate, ElementName=DueDatePicker}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- The actual DatePicker — transparent background so pill shows through -->
                        <DatePicker x:Name="DueDatePicker"
                                    SelectedDate="{Binding DueDate, Mode=TwoWay}"
                                    Foreground="White"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    VerticalAlignment="Center"
                                    AutomationProperties.Name="Due Date">
                            <DatePicker.Resources>
                                <Style TargetType="DatePickerTextBox">
                                    <Setter Property="Foreground"               Value="White" />
                                    <Setter Property="Background"               Value="Transparent" />
                                    <Setter Property="BorderThickness"          Value="0" />
                                    <Setter Property="FontSize"                 Value="14" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DatePickerTextBox">
                                                <ScrollViewer x:Name="PART_ContentHost"
                                                              Foreground="White"
                                                              Background="Transparent"
                                                              VerticalAlignment="Center" />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DatePicker.Resources>
                        </DatePicker>
                    </Grid>
                </Border>
            </Grid>

            <!-- Save + Cancel row -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,4,0,0">
                <Button Content="Save"
                        Style="{StaticResource ActionButtonStyle}"
                        Command="{Binding SaveTaskCommand}"
                        Margin="0,0,12,0"
                        AutomationProperties.Name="Save Task" />
                <Button Content="Cancel"
                        Style="{StaticResource ActionButtonStyle}"
                        Command="{Binding CancelTaskCommand}"
                        AutomationProperties.Name="Cancel Task" />
            </StackPanel>

        </StackPanel>

        <!-- Row 3: task list -->
        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      Margin="60,0,60,20">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource CustomScrollbarStyle}" />
            </ScrollViewer.Resources>
            <Grid>
                <!-- Task items -->
                <ItemsControl ItemsSource="{Binding Tasks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#1a1a2e"
                                    CornerRadius="8"
                                    Padding="16"
                                    Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="White"
                                               FontWeight="Bold"
                                               FontSize="14" />
                                    <TextBlock Text="{Binding Description}"
                                               Foreground="#aaaaaa"
                                               FontSize="12"
                                               TextWrapping="Wrap" />
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding AssignedTo}"
                                                   Foreground="#888888"
                                                   FontSize="11"
                                                   Margin="0,0,8,0" />
                                        <TextBlock Text="{Binding DueDate}"
                                                   Foreground="#888888"
                                                   FontSize="11" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Status}"
                                               Foreground="{StaticResource PurpleAccentBrush}"
                                               FontSize="11" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <!-- Empty state message -->
                <TextBlock Text="No tasks yet."
                           Foreground="#888888"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="0,24,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Tasks.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </ScrollViewer>

    </Grid>

</UserControl>
