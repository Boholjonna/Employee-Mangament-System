<Window x:Class="SOFTDEV.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SOFTDEV"
        mc:Ignorable="d"
        Title="Login"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#0a0a0a"
        ResizeMode="CanResize">

    <Grid Background="#0a0a0a">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1.2*" />
        </Grid.ColumnDefinitions>

        <!-- ═══════════════════════════════════════════════════════════
             Column 0 — Left Decorative Panel
             ═══════════════════════════════════════════════════════════ -->
        <Border Grid.Column="0"
                CornerRadius="25"
                Margin="20"
                Background="#1a1a2e">
            <Image Source="/images/login-bg.png"
                   Stretch="UniformToFill"
                   Opacity="0.6" />
        </Border>

        <!-- ═══════════════════════════════════════════════════════════
             Column 1 — Right Form Container
             ═══════════════════════════════════════════════════════════ -->
        <Border Grid.Column="1"
                Background="#15151b"
                CornerRadius="20"
                Margin="20"
                Padding="40,30">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />  <!-- Row 0: Header        -->
                    <RowDefinition Height="Auto" />  <!-- Row 1: Subtitle      -->
                    <RowDefinition Height="*"    />  <!-- Row 2: Input fields  -->
                    <RowDefinition Height="Auto" />  <!-- Row 3: Buttons       -->
                    <RowDefinition Height="Auto" />  <!-- Row 4: Footer        -->
                </Grid.RowDefinitions>

                <!-- ─────────────────────────────────────────────────
                     Language Button (top-right overlay, Row 0)
                     ───────────────────────────────────────────────── -->
                <Button x:Name="LanguageButton"
                        Grid.Row="0"
                        Content="EN"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Style="{StaticResource PillButtonStyle}"
                        Width="50"
                        Height="30"
                        Click="LanguageButton_Click">
                    <Button.ContextMenu>
                        <ContextMenu x:Name="LanguageContextMenu">
                            <MenuItem Header="EN" Click="LanguageMenuItem_Click" />
                            <MenuItem Header="FR" Click="LanguageMenuItem_Click" />
                            <MenuItem Header="DE" Click="LanguageMenuItem_Click" />
                            <MenuItem Header="ES" Click="LanguageMenuItem_Click" />
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <!-- ─────────────────────────────────────────────────
                     Row 0 — Header
                     ───────────────────────────────────────────────── -->
                <TextBlock Grid.Row="0"
                           Text="Hi, there!"
                           Foreground="#7b61ff"
                           FontSize="28"
                           FontWeight="Bold" Margin="-6,-2,7,1" />

                <!-- ─────────────────────────────────────────────────
                     Row 1 — Subtitle
                     ───────────────────────────────────────────────── -->
                <TextBlock Grid.Row="1"
                           Text="Welcome to SOFTDEV Portal"
                           Foreground="#aaaaaa"
                           FontSize="14"
                           Margin="0,4,0,24" />

                <!-- ─────────────────────────────────────────────────
                     Row 2 — Input Fields
                     ───────────────────────────────────────────────── -->
                <StackPanel Grid.Row="2">

                    <!-- User Role -->
                    <TextBlock Text="User Role"
                               Foreground="#7b61ff"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <Border Background="#2a2a3e"
                            CornerRadius="8"
                            Padding="12,10"
                            Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox x:Name="RoleEmployee"
                                      Content="Employee"
                                      Style="{StaticResource RoleCheckBoxStyle}"
                                      Margin="0,0,24,0"
                                      Checked="RoleCheckBox_Checked"
                                      Unchecked="RoleCheckBox_Unchecked" />
                            <CheckBox x:Name="RoleManager"
                                      Content="Manager"
                                      Style="{StaticResource RoleCheckBoxStyle}"
                                      Margin="0,0,24,0"
                                      Checked="RoleCheckBox_Checked"
                                      Unchecked="RoleCheckBox_Unchecked" />
                            <CheckBox x:Name="RoleAdmin"
                                      Content="Admin"
                                      Style="{StaticResource RoleCheckBoxStyle}"
                                      Checked="RoleCheckBox_Checked"
                                      Unchecked="RoleCheckBox_Unchecked" />
                        </StackPanel>
                    </Border>

                    <!-- Username -->
                    <TextBlock Text="Employee User Name"
                               Foreground="#7b61ff"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <TextBox x:Name="UsernameTextBox"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             Margin="0,0,0,16"
                             MaxLength="100" />

                    <!-- Email -->
                    <TextBlock Text="Email ID"
                               Foreground="#7b61ff"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <TextBox x:Name="EmailTextBox"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             Margin="0,0,0,16"
                             MaxLength="200" />

                    <!-- Password -->
                    <TextBlock Text="Password (min 8 char)"
                               Foreground="#7b61ff"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <PasswordBox x:Name="PasswordBox"
                                 Style="{StaticResource RoundedPasswordBoxStyle}"
                                 Margin="0,0,0,8" />

                    <!-- Inline validation error -->
                    <TextBlock x:Name="ErrorMessageText"
                               Foreground="#ff6b6b"
                               FontSize="12"
                               Visibility="Collapsed"
                               Margin="0,0,0,8" />

                </StackPanel>

                <!-- ─────────────────────────────────────────────────
                     Row 3 — Action Buttons
                     ───────────────────────────────────────────────── -->
                <StackPanel Grid.Row="3" Margin="0,16,0,0">

                    <Button Content="Log in"
                            Style="{StaticResource PillButtonStyle}"
                            Height="44"
                            Margin="0,0,0,12"
                            Click="LoginButton_Click" />

                    <Button Content="Sign in with Google"
                            Style="{StaticResource OutlinePillButtonStyle}"
                            Height="44"
                            Click="GoogleSignInButton_Click" />

                </StackPanel>

                <!-- ─────────────────────────────────────────────────
                     Row 4 — Footer Sign-Up Link
                     ───────────────────────────────────────────────── -->
                <TextBlock Grid.Row="4"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0">
                    <Run Text="Don't have an account? " Foreground="#888888" />
                    <Hyperlink Click="SignUpLink_Click">
                        <Run Text="Sign up" Foreground="#7b61ff" />
                    </Hyperlink>
                </TextBlock>

            </Grid>
        </Border>

    </Grid>
</Window>
